import { TaskDetailsHostDirective } from './task-details-host.directive';

describe('TaskDetailsHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskDetailsHostDirective();
    expect(directive).toBeTruthy();
  });
});
